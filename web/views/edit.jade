extends layout

block content
  h1= title

  div#editor_holder

  script.
    var element = document.getElementById('editor_holder');
    var editor = new JSONEditor(element, {
      schema: !{schema},
      theme: "bootstrap3",
      iconlib: "bootstrap3",
    });
